# Supabase Postgres connection (Project Settings → Database → Connection string → URI).
# DATABASE_URL = pooled connection, port 6543, pgbouncer=true — the app's runtime queries.
# DIRECT_URL   = direct connection, port 5432 — `prisma migrate` needs this one; migrations
#                don't work reliably through the pgbouncer pooler.
#
# KEEP THE FUNCTIONS IN THE SAME REGION AS THIS DATABASE. Production Postgres lives in
# aws-1-ap-south-1 (Mumbai), so vercel.json pins "regions": ["bom1"]. It used to default to
# iad1 (Washington DC), which made every query pay a round trip across half the planet — and
# these screens issue a lot of queries. That distance is what turned an ordinary query
# fan-out into the P2024 connection-pool exhaustion on /fan-pages (PR #26): a query that
# would return in single-digit milliseconds locally instead held its pool connection for
# hundreds, so a pool of five ran out far sooner than the query count alone suggests.
# If this database is ever moved, move the region with it.
DATABASE_URL="postgresql://postgres:password@localhost:5432/staranalytics"
DIRECT_URL=""

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-openssl-rand--base64-32"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Shared secret every /api/cron/* route requires in an `Authorization: Bearer <secret>`
# header. There are 8 of them now: poll-hashtags, backfill-sentiment,
# backfill-comment-sentiment, poll-scout-runs, scan-theater-campaigns, snapshot-buzz,
# prune-raw-payloads, weekly-digest. (All but backfill-sentiment are scheduled in
# vercel.json; backfill-sentiment is switched off for credit reasons — see
# APIFY-USAGE-AUDIT.md, "Disabled cron jobs".)
# Each route fails closed (500) if this is unset — generate with
# `openssl rand -base64 32` and set the same value in Vercel's cron config.
CRON_SECRET=""

# Data-minimization cron (prune-raw-payloads), DATA-PRIVACY.md "Retention".
# Both are policy decisions, not engineering ones — defaulted to 90 days each
# since nothing in the codebase implied a real number; change if wrong for how
# this data actually gets used.
# RAW_PAYLOAD_RETENTION_DAYS — nulls posts.raw / post_comments.raw (the full
#   Apify scrape blob; never read back after ingest-time extraction).
# COMMENT_RETENTION_DAYS — nulls post_comments.text / .author_handle (the one
#   structured-data column holding a third party's — the commenter's, not the
#   tracked account's — personal data; only ever read once, by the sentiment
#   classifier, and never again after).
RAW_PAYLOAD_RETENTION_DAYS="90"
COMMENT_RETENTION_DAYS="90"

# Max comments requested per post from apify/instagram-comment-scraper (src/lib/providers/
# apify-public-content.ts). No hard ceiling on the actor's side — real output is bounded
# only by how many comments Instagram exposes to a logged-out viewer, not by this number.
# Cost is pay-per-result (measured $2.30/1000 comments as of 2026-07-31 on this account's
# Starter plan). Was briefly uncapped (100000) the same day; reverted to 200 after large
# comment threads both burned a $29/month budget in under a day and pushed individual
# scrapes long enough to trigger silent invocation deaths (stuck locks, no error logged).
# (Defined once, here — it used to also appear in the Apify block below, where a second
# copy could drift out of sync with this one.)
COMMENTS_PER_POST_LIMIT="200"

# Global kill switch for FETCHING new comments from Apify. Off unless set to exactly "on" —
# "true"/"1"/"yes" all mean off, deliberately: one exact opt-in value is easier to grep for
# and reason about than a truthiness rule, and the cost of guessing wrong here is money.
#
# "Off" does NOT mean sentiment stopped. Classification of comments already stored keeps
# running (backfill-comment-sentiment, every minute), and the fan-page screens opt back in
# explicitly per call — which is the only reason their comment panels have anything to show.
# The cron path deliberately does not opt in; an unattended hourly opt-in would switch this
# pipeline back on by the back door. See src/lib/data/sentiment.ts (isCommentScrapeEnabled).
COMMENT_SCRAPE="off"

# Login allowlist — StarAnalytics is Confidential; plain Google OAuth is not enough.
# Set ONE of these before deploying anywhere real:
ALLOWED_EMAIL_DOMAIN=""
ALLOWED_EMAILS=""

# Per-provider data mode: mock | live.
# Flip DATA_MODE_APIFY to "live" once APIFY_TOKEN below is set — that's the switch that
# makes PublicContentProvider resolve to the real implementation instead of Mock.
DATA_MODE_INSTAGRAM="mock"
DATA_MODE_APIFY="mock"
DATA_MODE_SENTIMENT="mock"
DATA_MODE_NOTIFIER="mock"
DATA_MODE_YOUTUBE="mock"

# Sentiment CLASSIFICATION on/off, independent of the mode above. Off unless set to exactly
# "on". Off means no AI call and no Sentiment row written — not mock labels: DATA_MODE_SENTIMENT
# "mock" marks every post positive at 0.78 and stores it in the same table as real results,
# so it is the wrong way to switch this off. Comment scraping is unaffected either way, and
# anything skipped is picked up automatically on the first run after this flips to "on".
# Currently off in production: all three providers (Claude, OpenAI, Gemini) are out of credit.
SENTIMENT_CLASSIFY=""

# apify.com → Settings → Integrations → API token
APIFY_TOKEN=
# Apify spend controls — see APIFY-USAGE-AUDIT.md. All optional; defaults shown.
# Hard per-run charge cap, enforced by Apify itself via maxTotalChargeUsd.
APIFY_MAX_CHARGE_USD_PER_RUN=6
# Monthly headroom we refuse to spend into, so the account never reaches its plan cap.
APIFY_MONTHLY_RESERVE_USD=1
# Comment scraping: resultsLimit is PER URL, so run cost is posts x COMMENTS_PER_POST_LIMIT.
APIFY_COMMENT_POSTS_PER_RUN=10
APIFY_COMMENT_POSTS_PER_INVOCATION=20
APIFY_COMMENT_RUN_WAIT_MS=180000
# Biggest recurring line item: this many results per hashtag, every poll-hashtags tick.
APIFY_HASHTAG_RESULTS_LIMIT=150
APIFY_HANDLE_POSTS_LIMIT=50
APIFY_DATASET_ITEM_LIMIT=10000
# How long the quota circuit breaker stays open after Apify rejects a start with
# "platform-feature-disabled" (monthly cap exceeded). Default 55 minutes — just under the
# hourly poll, so a raised cap brings the pipeline back on its own without a deploy.
# See src/lib/apify/quotaBreaker.ts and APIFY-USAGE-AUDIT.md finding E.
APIFY_QUOTA_COOLDOWN_MINUTES=55

# Which Apify actor(s) back PublicContentProvider's live implementation.
# APIFY_ACTOR_HASHTAG  — apify/instagram-hashtag-scraper, used by scrapeByHashtag.
# APIFY_ACTOR_PROFILE  — apify/instagram-profile-scraper, used for followers/display-name
#                        snapshots (fetchProfileSnapshot).
# APIFY_ACTOR_POST     — apify/instagram-post-scraper, used by both scrapeByHandle's
#                        post-history call and scrapeByUrls (single `username` input
#                        field accepts usernames, profile URLs, or post URLs).
APIFY_ACTOR_HASHTAG=""
APIFY_ACTOR_PROFILE=""
APIFY_ACTOR_POST=""
# apify/instagram-comment-scraper — comment ingestion for the sentiment pipeline (Phase 4).
# Only invoked for posts entering sentiment classification, never for every scraped post,
# and only when COMMENT_SCRAPE is "on" or the caller opts in explicitly (see above).
APIFY_ACTOR_COMMENTS=""

# Scoutline (bulk talent scan) actors. Defaults shown are what the code falls back to when
# these are unset, so Scoutline works without setting either.
# APIFY_ACTOR_SCOUT          — per-account engagement/reach analytics for Instagram handles.
# APIFY_ACTOR_SCOUT_FACEBOOK — Facebook support is PAGES ONLY (added 2026-08-18); personal
#                              profiles are out of scope, not unimplemented.
APIFY_ACTOR_SCOUT="easy_scraper/instagram-profile-engagement-analytics"
APIFY_ACTOR_SCOUT_FACEBOOK="apify/facebook-pages-scraper"

# Phase 4 sentiment: direct Claude API call (plain fetch, no SDK/orchestration infra).
# console.anthropic.com -> API Keys
ANTHROPIC_API_KEY=""
ANTHROPIC_SENTIMENT_MODEL="claude-sonnet-5"

# Cross-provider sentiment fallback (2026-07-31): if Claude's call fails for any reason
# (rate limit, exhausted credit balance, network error), the pipeline automatically retries
# with OpenAI, then Gemini. All three are required for the fallback to actually cover a
# Claude outage — an unset key just means that leg of the chain throws immediately and the
# next provider is tried. platform.openai.com / aistudio.google.com for keys.
#
# PRIVACY NOTE: this means third-party comment text can reach OpenAI and Google, not only
# Anthropic. DATA-PRIVACY.md's "Cross-border processing" section names all three.
OPENAI_API_KEY=""
OPENAI_SENTIMENT_MODEL="gpt-5.6-terra"
GEMINI_API_KEY=""
GEMINI_SENTIMENT_MODEL="gemini-3.5-flash"

# How many classification batches run concurrently (both post- and comment-level).
SENTIMENT_BATCH_CONCURRENCY=5

# Phase 5 fan-page alerts: real delivery via Resend (plain fetch, no SDK). Flip
# DATA_MODE_NOTIFIER to "live" once these are set — stays on the mock/console notifier
# until then. ALERT_EMAIL_FROM must be a sender Resend will actually accept: either a
# domain you've verified with Resend, or their sandbox address "onboarding@resend.dev"
# (sandbox mode only delivers to the email your Resend account was signed up with).
RESEND_API_KEY=""
ALERT_EMAIL_FROM=""
ALERT_EMAIL_TO=""

# Phase 7 self-account dashboard: Instagram Graph API (Instagram Login), direct fetch, no
# SDK. Gated on Meta App Review — a Business/Creator IG account, a registered Meta app,
# and approved permissions (instagram_basic, instagram_manage_insights) are prerequisites.
# INSTAGRAM_ACCESS_TOKEN         — long-lived access token for that app/account.
# INSTAGRAM_BUSINESS_ACCOUNT_ID  — the IG User ID (graph.facebook.com/{id}), not the @handle.
# Flip DATA_MODE_INSTAGRAM to "live" once both are set.
INSTAGRAM_ACCESS_TOKEN=""
INSTAGRAM_BUSINESS_ACCOUNT_ID=""

# Theater Campaign Intelligence — BookMyShow demand-pressure tracking.
# See BOOKMYSHOW-FEASIBILITY.md before enabling any of this.
#
# DATA_MODE_BOOKMYSHOW — mock | live. Keep on "mock" until an Apify browser actor has been
#   PROVEN to load a BookMyShow showtime page: every observation behind this feature was
#   made from a residential browser, and it is still unknown whether BookMyShow serves
#   these pages to a datacenter IP. Mock mode is fully functional — it replays the real
#   Kerala measurements from 2026-08-20 — so the UI works with no Apify account at all.
# BOOKMYSHOW_MONITORING_ENABLED — deliberately SEPARATE from the mode switch. "live" means
#   a real scan works; this means unattended scheduled scanning is allowed to run. Keep it
#   false while testing, so a manual "Scan now" can be exercised without also starting a
#   cron that hits BookMyShow every 90 minutes.
DATA_MODE_BOOKMYSHOW="mock"
BOOKMYSHOW_MONITORING_ENABLED="false"
# Which Apify actor renders the page. Must be a JS-rendering actor: BookMyShow serves an
# empty React shell to a plain HTTP GET, and the showtime data only exists after hydration.
# NOTE: apify/web-scraper and apify/puppeteer-scraper both require a one-time permission
# approval in the Apify console before they will run at all.
APIFY_ACTOR_BOOKMYSHOW="apify/web-scraper"
# How many days ahead each scan covers, and how many pages it renders concurrently.
# Concurrency is kept low on purpose — this is a third party's site, and a burst is the
# thing most likely to get the whole approach blocked.
BOOKMYSHOW_SCAN_HORIZON_DAYS=3
BOOKMYSHOW_SCAN_CONCURRENCY=3
# Wait budget for one scan run. Must stay below the scan route's maxDuration or the
# function is killed mid-wait and the Apify run is orphaned — billed, never read.
BOOKMYSHOW_RUN_WAIT_MS=480000
# Spend guards for a scan run, enforced by Apify itself. Sized from page count with
# headroom: a cap that truncates a Kerala-wide scan would silently produce a partial
# picture, which is worse than spending a few cents more.
BOOKMYSHOW_CHARGE_PER_PAGE_USD=0.02
BOOKMYSHOW_MAX_CHARGE_USD=10
# Opt-in live integration test. Never runs by default; needs APIFY_TOKEN as well.
RUN_BOOKMYSHOW_INTEGRATION_TEST="false"

# --- Local capture (scripts/bms-capture.mjs) ---
# BookMyShow blocks server-side collection (verified 2026-08-20: Apify headless and curl
# both get 403 while a real browser on the same connection gets 200), so data is gathered
# by a real Chrome on an operator's machine and POSTed to the ingest endpoint.
#
# BOOKMYSHOW_CAPTURE_SECRET — shared secret between the script and the ingest endpoint.
#   The ingest and capture-plan routes are the only campaign endpoints not behind NextAuth,
#   because a scheduled script has no browser session; this is what protects them. Both
#   fail closed when it is unset. Generate with `openssl rand -base64 32`.
BOOKMYSHOW_CAPTURE_SECRET=""
# Where the script POSTs. The app's own base URL.
STARANALYTICS_URL="http://localhost:3000"
# Server-side ceiling on captures per campaign per 24h. The script paces itself, but a
# client-side limit is only a suggestion — this is the one that holds, and it is what keeps
# collection at the human-scale volume the whole approach depends on.
BOOKMYSHOW_CAPTURE_MAX_PER_DAY=6
# Script defaults. Cities default to the campaign's own configured list.
BOOKMYSHOW_CAPTURE_CITIES=""
BOOKMYSHOW_CAPTURE_DAYS=2
BOOKMYSHOW_CAPTURE_DELAY_MS=4000
# Which campaign/title the script captures. Each has a CLI equivalent that wins over the
# env var (--campaign, --event-code, --movie-slug); the env form is for the scheduled task,
# where there is no command line to pass them on. BOOKMYSHOW_CAPTURE_CAMPAIGN is required —
# the script exits rather than guessing which campaign a capture belongs to.
BOOKMYSHOW_CAPTURE_CAMPAIGN=""
BOOKMYSHOW_CAPTURE_EVENT_CODE=""
BOOKMYSHOW_CAPTURE_MOVIE_SLUG=""

# YouTube (competitor/fan-channel tracking) — official Data API v3, direct fetch, no SDK.
# Just a self-service API key (no app-review process): console.cloud.google.com → enable
# "YouTube Data API v3" → Credentials → create an API key. Flip DATA_MODE_YOUTUBE to "live"
# once set. Own-channel private analytics (impressions, demographics) would need a
# separate OAuth-based YouTube Analytics API integration — not built yet, deferred like
# Phase 7's Meta gate.
YOUTUBE_API_KEY=""
